This directory is meant for testing.

`make` runs all exercises and converts them into PDFs.

Note that you need to set the environment variable `ARTS_DATA_PATH`
correctly to the locations of the catalogs.
